<%= form_with(model: [post, post.comments.build]) do |f| %>
  <div class="form-control mt-4">
    <%= f.label :body, "Add a Comment", class: "form-label" %>
    <%= f.rich_text_area :body, class: "trix-control" %>
    <%= f.submit "Replay", class: "btn btn-primary mt-1" %>
  </div>
<% end %>